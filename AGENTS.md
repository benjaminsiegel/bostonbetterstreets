# Publishing workflow

- When the site owner asks to push, publish, add, or update website content, validate the change and push it directly to `main` by default.
- Do not create a pull request unless the owner explicitly asks for a preview, review, or pull request.
- Before publishing, run the relevant production build and preserve any unrelated local changes.
- Treat `main` as the live Vercel production branch.
